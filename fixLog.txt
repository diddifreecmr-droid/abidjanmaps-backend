2026-07-23 20:36:37
Changement: Creation de guide.txt pour expliquer le projet fichier par fichier.
Effet: Le code devient plus facile a comprendre pour un nouveau dev.
Pourquoi: Cela aide a suivre l'architecture sans se perdre dans les dossiers.

2026-07-23 21:12:52
Changement: Ajout de tests pour les endpoints roads, places et route_reports, puis installation de geoalchemy2 dans le venv.
Effet: Les nouveaux endpoints CRUD sont verifies automatiquement et la partie PostGIS peut s'importer correctement dans l'environnement de test.
Pourquoi: Cela valide que la base technique des donnees geographiques est stable avant d'aller plus loin.

2026-07-23 21:30:46
Changement: CORS dev ouvert pour localhost et reseaux WLAN, plus recherche de places par nom ou alias.
Effet: Le frontend local ou sur le WLAN peut appeler l'API, et les lieux peuvent etre retrouves plus facilement.
Pourquoi: Cela prepare la phase 2 avec plus de confort pour le developpement et la recherche de lieux locaux.

2026-07-23 21:32:54
Changement: Ajout d'un docker-compose.override.yaml pour le mode developpement avec --reload.
Effet: En dev, le backend recharge automatiquement le code quand un fichier change.
Pourquoi: On garde un Docker propre pour la stack, tout en ayant le confort du hot reload pendant le dev.

2026-07-23 21:32:54
Changement: Correction du healthcheck OSRM dans docker-compose.yaml.
Effet: Docker verifie maintenant une vraie route OSRM au lieu d'un endpoint /health qui peut ne pas exister.
Pourquoi: Cela evite que Compose marque OSRM comme unhealthy alors que le service repond correctement aux requetes de route.

2026-07-23 21:55:13
Changement: Retrait du healthcheck OSRM du docker-compose.yaml.
Effet: Compose attend seulement que le conteneur OSRM soit demarre, sans supposer l'existence d'un endpoint de sante.
Pourquoi: On evite de tester une route fictive et on preferera ajouter un vrai healthcheck plus tard si l'API OSRM le permet clairement.

2026-07-23 22:52:55
Changement: Enrichissement des modeles roads et places avec les champs terrain de phase 2.
Effet: Le backend peut maintenant stocker des infos comme surface reelle, praticabilite saisonniere, point de controle, largeur utile, vernacular name et statut de validation.
Pourquoi: Cela prepare la carte locale d'Abidjan avec des donnees plus utiles que les simples routes OpenStreetMap de base.

2026-07-23 23:09:12
Changement: Ajout de la validation proposed/validated/rejected et de l'historique pour roads et places.
Effet: Le backend peut maintenant valider ou rejeter une donnee et conserver une trace des changements.
Pourquoi: Cela permet de controler les donnees locales avant de les faire influencer le moteur de route.

2026-07-24 00:45:09
Changement: Preparation du backend pour retourner plusieurs propositions de routes OSRM.
Effet: Le backend expose maintenant un nouvel endpoint dedie aux alternatives de route et sait lire plusieurs routes dans la reponse OSRM.
Pourquoi: Cela permet de presenter plusieurs trajets possibles au frontend sans casser l'endpoint principal existant.

2026-07-24 00:56:00
Changement: Ajout d'un score backend et d'un classement pour les propositions de routes.
Effet: Chaque route alternative peut maintenant etre triee selon un score calcule avant affichage.
Pourquoi: Cela prepare l'utilisation future des enrichissements locaux pour penaliser ou favoriser certaines routes.

2026-07-24 01:10:58
Changement: Branchement des enrichissements roads et route_reports sur le score des alternatives de route.
Effet: Le backend recupere maintenant les troncons touches par chaque route, lit leurs attributs locaux et applique ces infos au score final.
Pourquoi: Cela permet de penaliser ou favoriser une route selon l'etat reel du terrain, sans refaire tout le calcul au moment de la requete.

2026-07-24 01:16:01
Changement: Ajout d'un score_breakdown plus lisible, d'un endpoint detaille pour les routes proposees et de traces d'historique quand un route_report touche une route.
Effet: Les reponses de routing montrent maintenant clairement les facteurs de score, les troncons touches et les reports relies, et l'historique garde une trace du recalcul metier.
Pourquoi: Cela aide a debugger, a expliquer pourquoi une route est classee avant une autre, et a garder un suivi propre des enrichissements locaux.

2026-07-24 01:18:24
Changement: Standardisation des valeurs metier pour surface_state et report_type avec normalisation des alias simples.
Effet: Le backend accepte des termes humains comme "ok" ou "peage", mais stocke une version canonique comme "good" ou "toll".
Pourquoi: Cela evite les incoherences de vocabulaire dans la base et rend le scoring plus fiable sur la duree.

2026-07-24 10:56:50
Changement: Standardisation aussi de seasonal_practicability, point_controle et type_flux, avec un endpoint taxonomy pour lister les valeurs autorisees.
Effet: Les donnees de route sont maintenant plus homogenes, et l'API peut montrer clairement quelles valeurs sont acceptees ou normalisees.
Pourquoi: Cela simplifie la saisie, evite les variantes inutiles dans la base et rend les regles metier plus faciles a maintenir.

2026-07-24 11:06:40
Changement: Ajout de l'initialisation automatique du schema SQL au demarrage du backend.
Effet: Si la base Docker est vide, les tables sont creees automatiquement avant les premieres requetes.
Pourquoi: Cela evite l'erreur "relation roads does not exist" quand on redemarre la stack sur une base neuve.

2026-07-24 12:15:00
Changement: Remplacement de create_all au demarrage par Alembic et ajout du service Docker init-db.
Effet: Les tables sont maintenant creees et mises a jour par des migrations versionnees avant le demarrage du backend. Le service init-db applique la migration puis se termine normalement.
Pourquoi: Cela rend les evolutions de schema previsibles, partageables entre developpeurs et plus sure pour les futures donnees metier. Une base deja creee avec create_all doit etre adoptee une seule fois avec alembic stamp head, sans supprimer ses donnees.

2026-07-24 12:25:00
Changement: Correction du chemin d'import utilise par Alembic dans Docker.
Effet: Le conteneur de migration peut maintenant charger les modeles du package app avant d'executer une migration.
Pourquoi: Alembic est lance par sa propre commande et ne recevait pas automatiquement le dossier du backend dans son chemin Python.

2026-07-24 12:32:00
Changement: Remplacement du healthcheck Docker du backend base sur wget par une requete Python standard.
Effet: Docker peut maintenant verifier correctement /api/v1/health dans l'image python:slim.
Pourquoi: wget n'est pas installe dans cette image, alors que Python et urllib font deja partie de l'environnement du backend.

2026-07-24 12:36:00
Changement: Installation d'Alembic dans l'environnement virtuel abidjanmaps-backend et adoption de la base existante au niveau 20260724_0001.
Effet: Les commandes Alembic fonctionnent maintenant en local et dans Docker, et la base actuelle est reconnue sans recreation des tables ni perte de donnees.
Pourquoi: Le projet peut desormais faire evoluer son schema avec des migrations versionnees a partir de la version initiale.

2026-07-24 12:50:00
Changement: Debut de la migration vers un monolithe modulaire vertical. Le module routing possede maintenant ses couches domain, application, infrastructure et presentation. Le coeur domain/application/infrastructure de map_data a aussi ete extrait.
Effet: Le code de calcul de route est regroupe par metier et les anciens chemins restent comme adaptateurs de compatibilite pendant la transition.
Pourquoi: Cette structure rend les domaines plus faciles a comprendre et prepare une separation future en services sans transformer prematurement le projet en microservices.

2026-07-24 13:10:00
Changement: Finalisation de la migration des trois modules metier routing, map_data et local_enrichment. Deplacement aussi de la configuration, base de donnees, logging et health checks communs vers shared.
Effet: Les tests utilisent maintenant les modules canoniques pour routing, map_data et local_enrichment. Les anciennes couches servent uniquement de compatibilite temporaire.
Pourquoi: Le nouveau code peut evoluer par domaine metier sans melanger les responsabilites, tout en gardant une transition sure et testee.

2026-07-24 14:18:00
Changement: Separation physique des modeles ORM et mappers PostGIS entre map_data et local_enrichment, avec une Base SQLAlchemy commune dans shared.
Effet: Chaque module possede maintenant ses tables et conversions GeoJSON/PostGIS. Alembic charge les deux modules dans une metadata unique et ignore les tables internes des extensions PostGIS/Tiger.
Pourquoi: Cela supprime la dependance metier au fichier global models.py, prepare une future extraction en services et empeche Alembic de proposer la suppression de tables systeme.

2026-07-24 14:18:00
Changement: Ajout de tests d'architecture pour verifier la propriete des modeles ORM et l'unicite de la metadata SQLAlchemy.
Effet: La suite compte maintenant 17 tests et detectera une double declaration de table ou un modele replace dans le mauvais module.
Pourquoi: La nouvelle architecture doit etre protegee automatiquement, pas seulement documentee.

2026-07-24 14:32:00
Changement: Separation des taxonomies entre map_data et local_enrichment, avec un endpoint /route-reports/taxonomy dedie.
Effet: Les routes et les signalements possedent maintenant chacun leur vocabulaire metier. /roads/taxonomy ne melange plus les deux domaines.
Pourquoi: Une frontiere de module doit inclure ses valeurs autorisees et ses regles de normalisation.

2026-07-24 14:32:00
Changement: Suppression complete des anciens dossiers horizontaux application, domain, infrastructure et presentation. Ajout d'un bootstrap explicite pour assembler les routers.
Effet: La seule arborescence de source est maintenant bootstrap, modules et shared. Les URLs HTTP existantes restent identiques.
Pourquoi: Les adaptateurs temporaires avaient rempli leur role; les conserver aurait entretenu deux architectures et rendu le projet confus.

2026-07-24 14:32:00
Changement: Renforcement des tests d'architecture et reecriture de guide.txt pour la nouvelle structure.
Effet: La suite compte 20 tests et echoue si un ancien dossier ou import horizontal reapparait.
Pourquoi: La restructuration doit rester durable quand plusieurs developpeurs ajouteront du code.

2026-07-24 16:41:58
Changement: Ajout du workflow complet proposed/validated/rejected pour les route_reports, avec endpoints de validation, rejet, filtrage par statut et historique dedie.
Effet: Un signalement nouvellement cree reste une proposition. Un administrateur peut ensuite le valider ou le rejeter en indiquant son identifiant et une note.
Pourquoi: Une information terrain non verifiee ne doit jamais modifier les itineraires proposes aux utilisateurs.

2026-07-24 16:41:58
Changement: Le scoring PostGIS ne charge maintenant que les routes et signalements validated. Correction aussi des controles de type none, de la securite inconnue et des signalements severes qui etaient trop facilement consideres comme bloquants.
Effet: Les propositions et rejets restent consultables pour l'audit mais sont ignores par le calcul. Un peage severe, par exemple, n'est plus confondu avec une route bloquee.
Pourquoi: Le classement doit reposer uniquement sur des informations publiees et sur une interpretation metier correcte.

2026-07-24 16:41:58
Changement: Les creations publiques de roads et places ne peuvent plus choisir elles-memes leur statut de validation. Ajout de la migration Alembic 20260724_0002.
Effet: Toute nouvelle donnee commence en proposed, et la base conserve le reviewer, la date de revue et l'historique des route_reports.
Pourquoi: Cela ferme un contournement du processus de validation et garde une trace comprehensible de chaque decision.

2026-07-24 17:03:50
Changement: Ajout des endpoints PATCH pour roads, places et route_reports, avec cas d'usage dedies et historique old_value/new_value.
Effet: Une information peut etre corrigee champ par champ sans renvoyer tout l'objet. Toute modification remet automatiquement son statut a proposed et efface la precedente validation du signalement.
Pourquoi: Une validation porte sur une version precise des donnees; apres une correction, la nouvelle version doit etre revue avant d'influencer le routage.

2026-07-24 17:03:50
Changement: L'indicateur verified des places est maintenant gere par le workflow de validation, et non par le client.
Effet: Il devient true lors de /validate, puis repasse false apres une modification ou un rejet.
Pourquoi: Cela evite qu'un utilisateur declare lui-meme un lieu verifie ou conserve une verification devenue obsolete.

2026-07-24 17:03:50
Changement: Ajout d'un test d'integration reel pour les workflows PATCH avec SQLAlchemy et PostGIS.
Effet: Le test couvre les vraies tables, geometries, transactions et historiques, puis effectue un rollback externe pour ne laisser aucune donnee de test.
Pourquoi: Les tests API rapides utilisent des repositories simules; ce second niveau detecte les erreurs qui ne peuvent apparaitre qu'avec PostgreSQL/PostGIS.

2026-07-24 17:58:29
Changement: Ajout des profils vehicule car, motorcycle et truck, avec les alias voiture, moto et camion, et migration Alembic 20260724_0003.
Effet: Chaque road stocke maintenant allowed_vehicle_profiles. Les anciennes routes autorisent les trois profils par defaut afin de conserver leur comportement.
Pourquoi: Le backend doit connaitre les vehicules acceptes sur un troncon avant de pouvoir recommander un itineraire adapte.

2026-07-24 17:58:29
Changement: Branchement de vehicle_width_m, vehicle_weight_t, width_usable_m et tonnage_max_reel_t dans le scoring des alternatives.
Effet: Une route interdite au profil, trop etroite ou limitee a un tonnage inferieur recoit un fort malus. score_breakdown indique forbidden, overweight, too_wide et eligible.
Pourquoi: Une route geometriquement calculable par OSRM n'est pas forcement praticable par le vehicule reel de l'utilisateur.

2026-07-24 17:58:29
Changement: Correction du rattachement entre une alternative triee et son detail d'enrichissement.
Effet: Apres classement, chaque route conserve maintenant ses propres troncons, signalements et contraintes vehicule.
Pourquoi: Un tri change l'ordre des routes; utiliser leur ancienne position pouvait afficher le malus d'une route sur une autre.

2026-07-24 18:32:07
Changement: Remplacement de l'intersection geographique exacte par ST_DWithin avec une tolerance configurable de 15 metres.
Effet: Une donnee locale peut enrichir une trace OSRM meme si les deux geometries ont un petit decalage. Les index GIST existants sont conserves; aucune migration d'index inutile n'a ete ajoutee.
Pourquoi: Deux sources cartographiques de la meme route ne produisent presque jamais des coordonnees parfaitement identiques.

2026-07-24 18:32:07
Changement: Ajout de quatre troncons de demonstration Phase 2 et d'un script de seed idempotent.
Effet: Le premier lancement cree et valide quatre roads. Les lancements suivants les reconnaissent avec seed_key et seed_version et ne creent aucun doublon.
Pourquoi: Le scoring avait besoin de donnees locales validees et controlees pour demontrer concretement ses decisions.

2026-07-24 18:32:07
Changement: Ajout d'un script de verification des scenarios car, motorcycle et truck contre le vrai OSRM et PostGIS.
Effet: La voiture et le camion preferent la branche large et evitent une alternative incompatible; la moto peut utiliser la branche courte.
Pourquoi: Ce test de bout en bout prouve que le calcul OSRM, le rapprochement spatial et le scoring metier travaillent ensemble.

2026-07-24 20:53:30
Changement: Ajout du module metier vertical users avec la table users et la migration Alembic 20260724_0004.
Effet: Les comptes possedent un email unique, un mot de passe hache, un role user/admin et un etat actif. Le premier administrateur se cree avec scripts.create_user.
Pourquoi: L'authentification devient un vrai domaine metier, isole et extractible plus tard, plutot qu'un ensemble de fonctions dispersees.

2026-07-24 20:53:30
Changement: Ajout de l'authentification JWT et du hachage Argon2, puis protection des endpoints d'ecriture.
Effet: Les lectures et le routage restent publics; les creations et modifications exigent un compte actif; validate, reject et la gestion des comptes exigent admin.
Pourquoi: Les donnees de cartographie influencent le scoring et ne doivent pas pouvoir etre publiees ou alterees anonymement.

2026-07-24 20:53:30
Changement: L'auteur des signalements, modifications et validations vient maintenant du jeton authentifie.
Effet: reported_by, changed_by et reviewed_by ne peuvent plus etre falsifies par le JSON du client. Des tests unitaires et un test PostgreSQL avec rollback couvrent ce workflow.
Pourquoi: Un historique n'est utile pour l'audit que si l'identite enregistree est controlee par le backend.

2026-07-25 12:35:13
Changement: Ajout de docker-compose.portainer.yaml pour le deploiement depuis GitHub via Portainer.
Effet: Le deploiement Portainer n'utilise plus le service one-shot init-db. Le backend applique les migrations Alembic au demarrage, puis lance FastAPI. Les variables importantes comme POSTGRES_PASSWORD, AUTH_SECRET_KEY et OSRM_DATA_PATH sont declarables dans Portainer.
Pourquoi: Portainer peut considerer un conteneur de migration qui se termine comme une erreur de stack. Cette version garde le docker-compose local intact, mais propose un chemin plus stable pour le serveur. Les fichiers OSRM restent hors Git et doivent etre montes depuis un dossier serveur.

2026-07-25 13:17:18
Changement: Ajout de DEPLOYMENT.md et lien depuis le README.
Effet: Le workflow dev/staging/main, les conventions de stacks Portainer, les domaines Nginx Proxy Manager, les variables serveur et la checklist de deploiement sont maintenant documentes.
Pourquoi: Le projet commence a passer d'un backend local a une vraie organisation multi-environnements. Documenter maintenant evite de perdre les conventions quand le frontend et les futurs services seront ajoutes.

2026-07-25 14:01:42
Changement: Ajout de l'option --reset-password au script scripts.create_user.
Effet: Sur staging ou production, on peut maintenant reinitialiser le mot de passe d'un utilisateur existant sans manipuler directement PostgreSQL. La documentation de deploiement rappelle que chaque environnement a sa propre base utilisateurs.
Pourquoi: Un compte cree en local n'existe pas automatiquement sur le VPS. Cette commande facilite la correction d'un login invalide apres un deploiement Portainer.

2026-07-25 14:14:50
Changement: Amelioration des erreurs de validation FastAPI.
Effet: Les requetes invalides retournent maintenant un message clair et un champ details avec le champ concerne, la raison et le type d'erreur. Un JSON mal forme indique explicitement "Invalid JSON body" au lieu du message placeholder.
Pourquoi: Les erreurs curl sous Windows peuvent venir de guillemets mal echappes. Le backend doit aider a comprendre si le probleme vient du JSON, d'un champ manquant ou des identifiants.

2026-07-25 14:36:21
Changement: Ajout de FRONTEND_BRIEFING.md et lien depuis le README.
Effet: Le developpeur frontend dispose maintenant d'un document complet sur les endpoints disponibles, les payloads, les reponses, les regles d'authentification, les use cases Phase 1/Phase 2 et les ecrans attendus.
Pourquoi: Le backend est assez avance pour servir de contrat API au frontend. Documenter les use cases maintenant evite que l'interface parte dans une direction differente du plan backend.

2026-07-27 10:34:28
Changement: Ajout des coordonnees dans les reponses de couches locales et creation de PHASE_STATUS.md.
Effet: Les endpoints roads, places et route_reports renvoient maintenant les geometries necessaires au frontend pour afficher les couches Phase 2 sur une carte. PHASE_STATUS.md resume ce qui est termine en Phase 1/2 et ce qui appartient a la Phase 3.
Pourquoi: Une donnee locale sans coordonnees est lisible dans une table, mais pas exploitable sur une carte. La Phase 2 doit permettre d'ajouter, valider, afficher et utiliser les enrichissements locaux.

2026-07-27 13:53:17
Changement: Assouplissement du script check_phase2_scenarios et ajout de TESTING_GUIDE.md.
Effet: Le script verifie maintenant que la meilleure proposition de chaque profil est eligible, sans imposer que toutes les alternatives moto le soient. TESTING_GUIDE.md clarifie les tests automatiques, les tests staging et les tests domaine.
Pourquoi: Les alternatives OSRM peuvent varier entre local et VPS. Le test doit valider la regle metier importante: detecter les incompatibilites et classer une route eligible en premier.

2026-07-27 14:30:32
Changement: Ajout du script scripts.check_staging_public_api.
Effet: On peut maintenant verifier rapidement depuis le conteneur backend que le domaine public staging expose health, db-health, roads, places, route-reports et routes/proposals/detail.
Pourquoi: check_phase2_scenarios valide surtout le scoring. Il fallait aussi un controle simple des endpoints que le frontend va consommer sur staging.

2026-07-27 14:40:09
Changement: Demarrage de la Phase 3 avec le module vertical journeys et la migration Alembic 20260727_0005.
Effet: Un utilisateur authentifie peut demarrer un trajet, envoyer des positions GPS, terminer le trajet, puis relire la trace. Les points sont stockes en PostGIS POINT et le backend calcule une premiere distance/duree reelle.
Pourquoi: La Phase 3 doit commencer par collecter proprement les donnees terrain avant de chercher a modifier automatiquement le graphe ou le scoring.
2026-07-27 15:05:07 - Documentation Phase 3 GPS

Ajout de `PHASE3_GPS_ANALYSIS.md` pour expliquer ce qu'est une trace GPS, ce
qu'on peut en extraire, et pourquoi on doit d'abord analyser/valider les traces
avant de modifier le Map Core.

Ajout de `FRONTEND_PHASE3_BRIEF.md` pour donner au developpeur frontend les use
cases Phase 3: demarrer une collecte, envoyer les positions GPS par batch,
terminer un trajet, afficher la trace reelle, puis se preparer a l'analyse GPS.

Mise a jour de `FRONTEND_BRIEFING.md`, `PHASE_STATUS.md`, `README.md` et
`TESTING_GUIDE.md` pour relier les nouveaux documents au reste du projet.

2026-07-27 15:05:07 - Analyse GPS Phase 3 V2

Ajout du stockage `journey_analyses` pour garder le resume d'analyse d'un trajet
GPS termine. Le backend peut maintenant calculer un score de qualite, une
distance exploitable, une duree, une vitesse moyenne backend, une vitesse moyenne
issue du telephone si disponible, les ecarts avec OSRM et des evenements simples
comme trace courte, mauvaise precision GPS, trajet beaucoup plus long que prevu
ou vitesse moyenne faible.

Ajout des endpoints `POST /api/v1/journeys/{journey_id}/analyze` et
`GET /api/v1/journeys/{journey_id}/analysis`. L'analyse ne modifie pas encore
les tables `roads` ou `route_reports`; elle produit seulement une observation
stockee pour preparer une validation metier plus tard.

La vitesse principale est calculee cote backend avec la distance entre deux
points GPS divisee par le temps entre ces deux points. Le champ `speed_mps`
envoye par le telephone reste une comparaison secondaire car il peut etre absent
ou bruite.

2026-07-27 15:24:46 - Documentation persistance DB et OpenAPI frontend

Ajout d'une section deploiement sur la protection des donnees PostgreSQL/PostGIS.
Le backend peut etre recree, mais les donnees vivent dans le volume Docker
`postgis_data`. Il ne faut donc jamais supprimer ce volume ou utiliser
`docker compose down -v` sans backup verifie.

Ajout d'une section Swagger/OpenAPI pour expliquer au frontend comment utiliser
`/docs` pour explorer l'API et `/openapi.json` pour generer des types TypeScript
ou un client API. Cela limite les erreurs de payload, de reponse et de
coordonnees.

2026-07-27 15:24:46 - Script de verification Phase 3 journeys

Ajout de `scripts.check_phase3_journeys`. Ce script teste un workflow API complet:
connexion avec un utilisateur existant, demarrage d'un trajet, envoi de positions
GPS, cloture du trajet, lecture du detail, analyse GPS et lecture de l'analyse
stockee. Il utilise les variables `PHASE3_TEST_EMAIL` et `PHASE3_TEST_PASSWORD`
pour eviter de coder un compte en dur.

2026-07-27 15:48:57 - Compatibilite Windows CMD pour check Phase 3

Correction du script `scripts.check_phase3_journeys` pour retirer les guillemets
accidentels autour des variables d'environnement. En Terminal Windows/CMD, une
commande comme `set BACKEND_BASE_URL="http://127.0.0.1:8000"` peut stocker les
guillemets dans la valeur, ce qui cassait httpx avec une URL qui ne commence plus
directement par `http://`.

Mise a jour de la documentation pour utiliser la forme CMD recommandee:
`set "NOM_VARIABLE=valeur"`. Cette forme evite que les guillemets deviennent une
partie de la valeur.

2026-07-27 15:48:57 - Migration Alembic automatique en developpement

Mise a jour de `docker-compose.override.yaml` pour lancer
`alembic upgrade head` avant `uvicorn --reload`. En developpement, l'override
remplacait la commande du backend et pouvait donc demarrer FastAPI sans appliquer
la derniere migration. Cela expliquait l'erreur `relation "journey_analyses" does
not exist` apres l'ajout de la migration Phase 3 V2.

La commande de secours documentee est:
`docker compose exec backend alembic upgrade head`.

2026-07-27 16:20:10 - Frontiere Map Core / DiddiGo pour la collecte GPS

Renommage du vocabulaire public de la collecte GPS Phase 3 vers `map-traces`.
Les nouveaux endpoints officiels sont maintenant `/api/v1/map-traces/...`.
Les anciens endpoints `/api/v1/journeys/...` restent disponibles mais caches de
Swagger pour compatibilite temporaire.

Le but est de garder cette fonctionnalite dans le domaine Map Core: collecte et
analyse de traces GPS pour ameliorer la carte. On evite ainsi de melanger avec
DiddiGo, qui gerera plus tard les courses, chauffeurs, passagers, dispatch,
paiement et suivi temps reel client.

2026-07-27 16:34:05 - Insights Map Core issus des traces GPS

Ajout de la table `map_trace_insights` et de la migration Alembic
`20260727_0007`. Quand une trace GPS est analysee, les evenements detectes sont
transformes en insights `proposed`, c'est-a-dire des observations a revoir.

Ajout des endpoints admin `GET /api/v1/map-trace-insights`,
`GET /api/v1/map-trace-insights/{id}`,
`POST /api/v1/map-trace-insights/{id}/validate` et
`POST /api/v1/map-trace-insights/{id}/reject`.

Ces insights ne modifient pas encore `roads`, `route_reports` ou le scoring.
Ils servent de sas de validation entre l'analyse GPS automatique et les futures
decisions cartographiques.

2026-07-27 16:34:05 - Nettoyage du contrat public trace_id

Remplacement de `journey_id` par `trace_id` dans les schemas publics de lecture
des positions GPS, des analyses et de la sortie du script
`check_phase3_map_traces`. Les noms internes et les tables DB gardent encore
`journey` volontairement, car la migration de renommage DB est reportee.

Ajout de filtres sur `GET /api/v1/map-trace-insights`: `status`,
`insight_type`, `severity_min` et `trace_id`. Ajout aussi de
`GET /api/v1/map-trace-insights/{id}/detail` pour retourner dans une seule
reponse l'insight, la trace et l'analyse associee.

2026-07-27 16:34:05 - Conversion controlee insight vers route_report

Ajout de `POST /api/v1/map-trace-insights/{id}/convert-to-route-report`.
L'endpoint exige un insight `validated`, puis cree un `route_report` en statut
`proposed`. Le report garde dans `extra_metadata` le lien vers l'insight, la
trace et l'analyse source.

La conversion ne valide pas le `route_report`; il doit encore passer par le
workflow normal de validation avant d'influencer le scoring.

2026-07-27 18:03:12 - Clarification OSRM driving et futur profil pieton

Ajout d'une note dans README.md, PHASE_STATUS.md et FRONTEND_PHASE3_BRIEF.md
pour clarifier que `osrm:5000` utilise actuellement le profil OSRM `driving`.
Les profils API `car`, `motorcycle` et `truck` restent des profils metier du
backend: ils influencent le prix, les contraintes et le scoring, mais utilisent
encore le meme moteur OSRM driving.

Le profil pieton/walking est note comme sujet futur. Il ne doit pas etre expose
au frontend avant d'avoir prepare des fichiers OSRM dedies et probablement un
service `osrm-walking`.

2026-07-27 18:07:45 - Enrichissement qualite des analyses GPS

Ajout de la migration Alembic `20260727_0008` pour stocker cinq nouveaux champs
dans `journey_analyses`: `moving_time_s`, `stopped_time_s`, `max_speed_kmh`,
`gps_gap_count` et `suspicious_jump_count`.

Le backend calcule maintenant le temps en mouvement, le temps a l'arret, la
vitesse maximale credible, les gros trous entre deux points GPS et les sauts GPS
improbables. Les sauts improbables sont filtres pour ne pas fausser la distance
et la vitesse moyenne.

Ces champs aident l'admin et le frontend a comprendre si une trace est fiable
avant de transformer un insight GPS en enrichissement Map Core.

2026-07-27 18:15:22 - Detection metier avancee des traces GPS

Ajout de nouveaux types d'evenements dans l'analyse GPS: `possible_slow_segment`,
`possible_blocked_road` et `possible_detour`. Ces evenements deviennent ensuite
des `map_trace_insights` proposes, comme les autres observations GPS.

`possible_slow_segment` signale une portion lente, `possible_blocked_road`
signale une route potentiellement bloquee ou tres difficile, et
`possible_detour` signale une trace reelle beaucoup plus longue que la route
prevue.

Ajout de tests pour verifier les trous GPS, les sauts GPS, les segments lents,
les routes possiblement bloquees et les detours. `possible_detour` peut etre
converti en `route_report` de type `degraded` apres validation admin.

2026-07-27 18:18:09 - Anti-doublons pour les insights Map Core

Ajout de la migration Alembic `20260727_0009` avec le champ `duplicate_key` sur
`map_trace_insights`. Cette cle combine le type d'insight et une zone
approximative de la trace. Exemple: `possible_slow_segment:-4.017:5.330`.

Le repository filtre maintenant les nouveaux insights avant insertion: si un
insight `proposed` ou `validated` existe deja avec la meme `duplicate_key`, le
backend ne repropose pas le meme probleme. Un insight `rejected` ne bloque pas
une future proposition, car plusieurs traces futures peuvent apporter une
nouvelle preuve.

Ajout d'un index unique partiel en base pour proteger aussi le staging et la
production contre les doublons concurrents. Le script `check_phase3_map_traces`
decale legerement sa trace de test a chaque execution pour rester repetable
malgre cette protection anti-doublons.

2026-07-27 18:42:50 - Confirmations terrain pour insights Map Core

Ajout de `evidence_count` et `latest_evidence_trace_id` sur les
`map_trace_insights`.

Pourquoi: un meme probleme detecte au meme endroit ne doit pas toujours etre
supprime comme un doublon. Si c'est la meme trace ou la meme analyse, c'est un
doublon technique. Si c'est une autre trace qui confirme le meme probleme dans
la meme zone, c'est une preuve terrain supplementaire.

Effet: le backend garde un seul insight actif par `duplicate_key`, mais augmente
`evidence_count` quand une trace differente confirme le signal. Cela prepare une
revue admin plus fiable sans encore faire confiance aveuglement aux traces GPS.

2026-07-27 18:53:14 - File de revue admin des insights Map Core

Ajout du tri sur `GET /api/v1/map-trace-insights` avec `sort` et `order`.
Valeurs de tri: `priority`, `severity`, `evidence`, `confidence` et `newest`.

Ajout de `GET /api/v1/map-trace-insights/review-queue`. Cette route renvoie les
insights les plus urgents a traiter, avec `review_priority_score`,
`recommended_action` et `conversion_ready`.

Ajout de `GET /api/v1/map-trace-insights/route-report-candidates`. Cette route
liste les insights deja valides, convertibles et assez solides pour proposer la
creation d'un `route_report`. Elle ne convertit rien toute seule: l'admin garde
le controle avec l'endpoint existant `convert-to-route-report`.

Pourquoi: on veut aider l'admin a travailler vite sans automatiser trop tot la
modification du Map Core. Le backend recommande, l'humain valide.

2026-07-27 19:35:48 - Script unique de validation staging

Ajout de `scripts.validate_staging`.

Ce script facilite la validation staging avec deux modes:

- `VALIDATE_STAGING_MODE=public`: teste seulement les endpoints publics comme
  health, db-health, roads, places, route_reports et propositions de routes;
- `VALIDATE_STAGING_MODE=full`: teste aussi le login, la collecte Map Trace,
  l'analyse GPS, les insights, la revue admin et la conversion en
  `route_report proposed`.

Le script retourne un JSON clair avec `status`, `mode`, `base_url`, chaque check
execute, sa duree et l'erreur lisible si quelque chose casse. Il peut etre lance
depuis le Terminal Windows contre l'URL staging ou directement dans le conteneur
backend sur le VPS/Portainer.

Pourquoi: une validation par simple lien URL ne suffit pas pour les workflows
qui creent des donnees et demandent une authentification. Le script donne donc
un controle reproductible sans exposer un endpoint public dangereux.

2026-07-27 20:30:22 - Correction validation staging places et insights deja confirmes

Correction de `/api/v1/places`: quand les places viennent de PostgreSQL/PostGIS,
`location` n'est pas un dict Python mais un objet geometrique SQLAlchemy. Le
backend utilise maintenant `ST_AsGeoJSON` dans ce cas pour convertir la position
avant de retourner `{lng, lat}`. Le bug ne se voyait pas en local quand la table
`places` etait vide, mais il cassait en staging avec de vraies lignes.

Correction de `scripts.check_phase3_map_traces`: le script accepte maintenant le
cas normal ou une nouvelle trace ne cree pas un nouvel insight parce qu'elle
confirme un insight actif existant via `latest_evidence_trace_id` et
`evidence_count`.

Effet: la validation staging devient compatible avec les donnees deja presentes
et avec la logique anti-doublons/confirmation terrain de Phase 3.

2026-07-28 12:48:52 - Import OSM de base et geocodage local

Ajout de `scripts.import_osm_base` pour importer depuis le fichier
`ivory-coast-latest.osm.pbf` deja utilise par OSRM.

Le script extrait les routes OSM nommees vers `roads` et les POI/places OSM
nommes vers `places`. Par defaut, il limite l'import a une bbox autour
d'Abidjan pour eviter d'importer toute la Cote d'Ivoire trop tot. Chaque ligne
garde son origine avec `extra_metadata.source = "osm"` ainsi que
`osm_type/osm_id`, ce qui rend l'import relancable sans creer de doublons.

Ajout de `GET /api/v1/roads/search?q=...` pour rechercher des rues/routes.

Ajout de `GET /api/v1/geocoding/search?q=...` comme endpoint recommande pour le
frontend. Il cherche a la fois dans `places` et `roads`, puis retourne un label,
un type et une coordonnee utilisable pour le calcul OSRM.

Le dossier OSRM est maintenant monte en lecture seule dans le conteneur backend
pour que le script puisse lire `/data/osrm/ivory-coast-latest.osm.pbf`.

Pourquoi: OSM donne une bonne base de noms, mais Diddi doit ensuite enrichir
avec les noms locaux, aliases, carrefours populaires et validations terrain.

2026-07-28 13:11:29 - Correction dependance native osmium dans Docker

Le package Python `osmium` etait bien installe dans le conteneur, mais il ne
pouvait pas demarrer car l'image `python:3.12-slim` ne contenait pas la librairie
systeme `libexpat.so.1`.

Ajout de `libexpat1` dans `backend/Dockerfile` avant l'installation des
dependances Python.

Pourquoi: certains packages Python ont une partie native en C/C++. `pip freeze`
peut donc montrer le package comme installe, mais l'import Python peut echouer si
une librairie Linux attendue par ce package manque dans l'image Docker.

2026-08-04 14:56:12 - Bias geographique pour la recherche locale

Ajout des parametres optionnels `bias_lat` et `bias_lng` sur les endpoints de
recherche `geocoding/search`, `places/search` et `roads/search`.

Quand les deux valeurs sont fournies, PostgreSQL/PostGIS trie les resultats avec
`ST_Distance` pour remonter les lieux et routes proches de l'utilisateur en
premier. Si un seul des deux parametres est fourni, le backend renvoie une erreur
claire car le bias doit toujours etre une coordonnee complete.

Pourquoi: l'autocomplete devient plus pertinent sans changer le contrat de base.
Le frontend peut continuer a chercher par texte simple, puis ajouter le bias des
que la position utilisateur est disponible.

2026-08-25 09:25:00 - Index trigram et index spatiaux pour la recherche

Ajout d'une migration Alembic `20260825_0011_search_trigram_indexes.py`.
Elle active l'extension PostgreSQL `pg_trgm`, puis ajoute des index GIN trigram
sur `places.name`, `places.vernacular_name`, `places.category` et `roads.name`.

Ajout aussi d'index GiST sur les colonnes geographiques principales:
`places.location`, `roads.geom` et `route_reports.geometry`.

Pourquoi: l'autocomplete et la recherche locale vont interroger beaucoup de
noms importes depuis OSM. Les index trigram accelerent les recherches partielles
comme `%plateau%`, et les index GiST accelerent les calculs geographiques comme
la proximite, le bias utilisateur et l'intersection avec un itineraire.

Ajout d'un test d'architecture qui verifie que cette migration garde bien les
index critiques. Ajout aussi d'un `tests/conftest.py` minimal pour rendre les
imports `app.*` stables pendant la collecte des tests, y compris dans
`tests/integration`.

Validation: `pytest` passe avec 66 tests OK, 1 test d'integration PostGIS saute
par defaut, et 2 warnings non bloquants.

2026-08-25 09:40:00 - Endpoint autocomplete geocoding pour le frontend

Ajout de `GET /api/v1/geocoding/autocomplete`.

Cet endpoint reutilise la recherche geocoding existante, mais renvoie une
reponse plus stable pour le frontend:
`status`, `query`, `count`, puis `results`.

Chaque resultat contient les champs utiles a une interface d'autocomplete:
`type`, `id`, `label`, `subtitle`, `category`, `location`, `source` et
`metadata`.

Regles ajoutees:
- minimum 2 caracteres pour eviter de lancer une recherche trop large;
- limite par defaut a 8 resultats;
- limite maximum a 20 resultats;
- support du bias geographique avec `bias_lat` et `bias_lng`;
- erreur claire si un seul des deux champs de bias est fourni.

Pourquoi: le frontend peut afficher directement les suggestions sans reconstruire
un format different pour les places et les routes. L'ancien endpoint
`/geocoding/search` reste disponible pour compatibilite.

Validation: ajout de tests sur le contrat autocomplete, la longueur minimale et
le bias incomplet. `pytest` passe avec 69 tests OK, 1 test d'integration PostGIS
saute par defaut, et 2 warnings non bloquants.

2026-08-25 09:55:00 - Validation routing reel Abidjan

Ajout du script `scripts/check_abidjan_routing.py`.

Ce script appelle le backend via `BACKEND_BASE_URL`, puis teste plusieurs trajets
connus dans Abidjan: Yopougon Siporex vers Plateau, Cocody Riviera vers Plateau,
Abobo vers Cocody, Aeroport vers Plateau, Marcory vers Treichville et Bingerville
vers Plateau.

Pour chaque trajet, il verifie:
- que le backend retourne au moins une proposition;
- que la geometrie est un GeoJSON `LineString` valide;
- que la distance est dans une fourchette plausible;
- que la duree est dans une fourchette plausible;
- que le scoring vehicule est present;
- que l'enrichissement local est charge.

Le script est aussi branche dans `scripts/validate_staging.py` comme verification
publique. Il ne demande pas de login, donc il peut tourner facilement en local,
dans Docker ou sur le VPS.

Ajout d'un test unitaire avec client HTTP fake pour garantir le format du rapport
JSON sans appeler OSRM pendant `pytest`.

Mise a jour de `FRONTEND_BRIEFING.md` avec le contrat autocomplete, le use case
depart/destination et l'usage attendu de `/api/v1/routes/proposals/detail`.

2026-08-25 10:15:00 - Preparation Docker pour validation terrain Map Core 1.4

Ajout d'un service Docker Compose `map-validation` dans les fichiers
`docker-compose.yaml` et `docker-compose.portainer.yaml`.

Ce service reutilise l'image backend et lance:
`python -m scripts.validate_staging`.

Il appelle le backend par le reseau interne Docker avec:
`BACKEND_BASE_URL=http://backend:8000`.

Par defaut, le mode est `VALIDATE_STAGING_MODE=public`, donc il teste les
endpoints publics, le routing reel Abidjan et les propositions de routes sans
demander de compte admin.

Si on definit `VALIDATE_STAGING_MODE=full` avec `PHASE3_TEST_EMAIL` et
`PHASE3_TEST_PASSWORD`, le meme service teste aussi le workflow des traces GPS:
demarrage de trace, envoi des positions, finish, analyse, insight, validation et
conversion en route_report.

Pourquoi: avant d'envoyer une version en staging ou avant une session avec les
chauffeurs, on peut lancer une verification Docker reproductible sans entrer
manuellement dans le conteneur.

Mise a jour de `README.md`, `DEPLOYMENT.md` et `FRONTEND_BRIEFING.md` avec les
commandes Docker, le role de DiddiGo et le pipeline GPS vers Map Core.

2026-08-25 10:35:00 - Protocole de test terrain DiddiMap puis DiddiGo

Ajout du document `FIELD_TEST_PROTOCOL.md`.

Le protocole separe volontairement les tests en deux grandes parties:

1. tester DiddiMap seul;
2. tester DiddiGo branche sur DiddiMap.

La partie DiddiMap couvre l'autocomplete, le calcul de route simple, les
alternatives avec scoring, les signalements terrain, les traces GPS, l'analyse
des traces et la revue admin des insights.

La partie DiddiGo explique ce que DiddiGo doit envoyer a DiddiMap: depart,
destination, profil vehicule, distance/duree prevues, geometrie prevue,
positions GPS, timestamps, precision GPS et vitesse telephone si disponible.

Le document rappelle la regle produit importante: une trace GPS ne modifie pas
directement la carte. Elle cree d'abord un insight, puis un admin valide ou
rejette, puis l'insight peut devenir un route_report qui devra lui aussi etre
valide avant d'influencer le scoring.

Mise a jour de `PHASE_STATUS.md` pour pointer vers ce protocole et resumer son
contenu.

2026-08-25 10:35:00 - Protocole test terrain Map Core 1.4

Ajout du document `FIELD_TEST_PROTOCOL.md`.

Ce document decrit comment organiser le test terrain avec 5 chauffeurs:
objectifs, roles, pre-requis techniques, donnees attendues depuis DiddiGo,
scenarios de test, trajets recommandes, volumes cibles, criteres de succes,
criteres d'echec, revue admin et livrables de fin de test.

Le protocole insiste sur une regle importante: une trace GPS ne modifie jamais
directement la carte. Elle produit d'abord un `map_trace_insight`, qui doit etre
valide par un admin avant de devenir un `route_report`, puis le `route_report`
doit lui aussi etre valide avant d'influencer le scoring.

Mise a jour de `README.md`, `DEPLOYMENT.md` et `PHASE_STATUS.md` pour pointer
vers ce protocole.
